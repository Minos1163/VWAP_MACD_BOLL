# Context Snapshot: auth-change

- Timestamp (UTC): `20260404T072806Z`
- Task statement: `clarify the auth change`
- Desired outcome: turn a vague auth-related change request into an execution-ready spec with explicit scope, non-goals, constraints, and decision boundaries.
- Stated solution: use deep interview first, then hand off to ralplan / ralph / team as appropriate.
- Probable intent hypothesis: the user wants to avoid starting a high-cost execution lane on an underspecified "auth change" and wants the true target clarified before planning or implementation.

## Known Facts / Evidence

- Repository is a brownfield Python crypto trading codebase with primary touchpoints under `src/`, `scripts/`, `tests/`, and `docs/`.
- No obvious first-class user-auth/login/session subsystem was found in initial project-scoped search.
- Evidence point 1:
  - `src/main.py` contains `_confirm_live_launch(...)` with `confirm_token`, `LIVE_CONFIRM_TOKEN`, and a second confirmation gate for starting live trading.
  - This looks like an operational safety confirmation flow, not end-user authentication.
- Evidence point 2:
  - `src/fund_flow/ai_weight_service.py` sends outbound API requests with `Authorization: Bearer {self.api_key}`.
  - This looks like external service credential usage, not an internal auth/login/session module.

## Constraints

- Do not assume the target is user login/auth unless the user confirms it.
- Minimize user effort: ask only one high-leverage question at a time.
- Preserve handoff readiness for `ralplan`, `ralph`, and `team`.
- Brownfield facts should be established before asking the user for internals.

## Unknowns / Open Questions

- What "auth" refers to in this repo:
  - live-trading launch confirmation
  - external API credential handling
  - a not-yet-located internal auth layer
  - a planned new auth feature outside current codebase patterns
- Why the change is needed now.
- What outcome would count as "done".
- What is explicitly out of scope.
- What OMX may decide without asking again.

## Decision-Boundary Unknowns

- Whether OMX may choose the auth mechanism/pattern itself.
- Whether backward compatibility is mandatory.
- Whether secret storage / env-var patterns may change.
- Whether UX/CLI surface changes are allowed.

## Likely Codebase Touchpoints

- `D:\AIDCA\AI8\src\main.py`
- `D:\AIDCA\AI8\src\fund_flow\ai_weight_service.py`
- `D:\AIDCA\AI8\src\api\`
- `D:\AIDCA\AI8\src\app\`
- `D:\AIDCA\AI8\tests\`
